{
  "name": "uap-files",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SIGINT — a pipeline-fed UAP/UFO files app with a ballistic world map and algorithm-computed bridge connections.",
  "scripts": {
    "predev": "node tools/sync-data.cjs",
    "dev": "vite",
    "prebuild": "node tools/sync-data.cjs",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "data": "cd scrapers && python3 scrape.py",
    "sync": "node tools/sync-data.cjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "vite": "^8.0.16"
  }
}
